/**
 * Minimal Jinja2 lexer/parser for client-side template syntax validation.
 * Vendored from @huggingface/jinja (MIT) — runtime/render code omitted.
 */
export { tokenize } from "./lexer";
export { parse } from "./parser";
