_ocmo:
  parameters:
    title:
      type: projected
      value: .Name
      description: Item name with transformer chain applied
      transformers:
        - slug
        - upper

result: "{!title}"
